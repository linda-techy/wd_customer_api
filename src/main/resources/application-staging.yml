# ╔══════════════════════════════════════════════════════════════════╗
# ║  Customer API — STAGING Profile                                  ║
# ║  🔑 SHARED STORAGE: same path as portal API for cross-app files ║
# ╚══════════════════════════════════════════════════════════════════╝

server:
  port: 8081
  error:
    include-message: never
    include-stacktrace: never

spring:
  datasource:
    url: jdbc:postgresql://46.202.164.251:5432/wdTestDB
    username: postgres
    password: Staygether@2025
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      leak-detection-threshold: 60000
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  task:
    execution:
      pool:
        core-size: 5
        max-size: 20
        queue-capacity: 200
      thread-name-prefix: async-cust-
    scheduling:
      pool:
        size: 3

  mail:
    host: smtp.hostinger.com
    port: 465
    username: info@walldotbuilders.com
    password: Wall.builders2018
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
  access-token-expiration: 3600000
  refresh-token-expiration: 604800000

# ── Shared storage (SAME path as portal-api staging) ────────────────
file:
  upload-dir: /home/ftpuser/var/www/app/walldotbuilders/storage
storageBasePath: /home/ftpuser/var/www/app/walldotbuilders/storage

app:
  email:
    enabled: true
  customer-portal-base-url: https://app.walldotbuilders.com
  cors:
    allowed-origins: https://app.walldotbuilders.com

logging:
  level:
    root: INFO
    com.wd.custapi: INFO
    org.springframework: WARN
    org.hibernate: WARN
